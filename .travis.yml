language: ruby

services:
  - docker

env:
  - DOCKER_TAG=19.9-stretch

before_script:
  - docker build -t webforgelabs/dlib:${DOCKER_TAG} .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
